%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%2345678901234567890123456789012345678901234567890123456789012345678901234567890
%        1         2         3         4         5         6         7         8

\documentclass[letterpaper, 10 pt, conference]{ieeeconf}  % Comment this line out if you need a4paper

%\documentclass[a4paper, 10pt, conference]{ieeeconf}      % Use this line for a4 paper

\IEEEoverridecommandlockouts                              % This command is only needed if 
                                                          % you want to use the \thanks command

\overrideIEEEmargins                                      % Needed to meet printer 
%\usepackage{graphics} % for pdf, bitmapped graphics files
%\usepackage{epsfig} % for postscript graphics files
%\usepackage{mathptmx} % assumes new font selection scheme installed
%\usepackage{times} % assumes new font selection scheme installed
%\usepackage{amsmath} % assumes amsmath package installed
%\usepackage{amssymb}  % assumes amsmath package installed
\usepackage{cite}
\usepackage{tikz}
\usepackage{amsmath}
\usepackage{tikz-3dplot}
\usetikzlibrary{arrows.meta, calc, fadings, decorations.markings, shapes.geometric, positioning, fit, backgrounds , shadows}
\usepackage{cleveref}
\usepackage{graphicx}
\usepackage{etoolbox}
\definecolor{inputblue}{RGB}{66, 133, 244}
\definecolor{networkpurple}{RGB}{103, 58, 183}
\definecolor{outputgreen}{RGB}{52, 168, 83}
\definecolor{blockfill}{RGB}{245, 247, 255}
\definecolor{gruorange}{RGB}{255, 152, 0}
\definecolor{grufill}{RGB}{255, 248, 235}
\definecolor{arrowblue}{RGB}{33, 150, 243}
\definecolor{transparentpink}{RGB}{233, 30, 99}
\definecolor{simColor}{RGB}{230, 240, 255} % 淺藍色 (模擬)
\definecolor{realColor}{RGB}{255, 240, 230} % 淺橘色 (真實)
\definecolor{networkColor}{RGB}{70, 70, 70} % 深灰色 (網絡)
\title{\LARGE \bf
Physics-Informed Deep Stereo: Active Asymmetric Polarization 
for Data-Efficient Transparent Obstacle Avoidance
}


\author{Po-Ting .Lin 
}


\begin{document}



\maketitle
\thispagestyle{empty}
\pagestyle{empty}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{abstract}


\end{abstract}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{INTRODUCTION}

Transparent obstacles represent a fundamental challenge for autonomous navigation and robotic manipulation. Their unique optical characteristics—specifically light transmission, specular reflection, and refraction distortion—render them effectively invisible or geometrically ambiguous to traditional sensing modalities.

Standard obstacle detection methods struggle significantly in this domain. LiDAR sensors fail as emitted pulses transmit through transparent surfaces rather than reflecting back \cite{tibebu_lidar-based_2021}. Conventional RGB-D cameras produce incomplete depth maps with large missing regions (holes) due to strong specular reflections and lack of texture \cite{lysenkov_recognition_2013}. Although ultrasonic sensors are unaffected by optical transparency, they lack the spatial resolution and directional precision required for dense 3D reconstruction \cite{borenstein_vector_1991}.

Recent learning-based methods have shown promise in addressing these limitations. Depth completion approaches train neural networks to infer complete depth maps from corrupted RGB-D inputs \cite{sajjan_clear_2020}. However, these methods are data-intensive, typically requiring tens of thousands of synthetic training images to learn the complex mapping from sparse depth to dense geometry. Active stereo methods \cite{shi_asgrasp_2024} leverage structured light patterns to mitigate textureless issues. While these approaches reduce dependency on scene texture, they remain vulnerable to the complex light transport (e.g., strong specular highlights) inherent to transparent materials, still necessitating extensive training data to generalize.

To address these limitations, we propose a Physics-Informed Deep Stereo (PIDS) framework for data-efficient transparent obstacle detection. Unlike symmetric stereo setups, our approach leverages an active asymmetric polarization configuration. We configure one camera to capture the parallel polarization state (preserving specular highlights) and the other to capture the orthogonal state (suppressing highlights). This setup creates a selective photometric discrepancy: background regions remain consistent across views, enabling reliable stereo matching, while transparent objects exhibit significant intensity differences. This physics-based input representation effectively converts the transparency detection problem into a discrepancy learning task. Consequently, our method enables existing stereo backbones to adapt to transparent scenes via data-efficient fine-tuning, eliminating the need for large-scale specialized datasets or complex architectural modifications.To summarize, our main contributions are:
\begin{itemize}
    \small
    \item \textbf{A Physics-Informed Sensing Modality}: We propose an active asymmetric polarization setup that converts transparent object detection into a robust photometric discrepancy task.
    \item \textbf{Data-Efficient Framework}: By leveraging physical cues, we demonstrate that standard stereo backbones can be adapted to transparent scenes with minimal real-world fine-tuning, significantly reducing data requirements compared to SOTA methods.
    \item \textbf{Generic Compatibility}: Our approach is model-agnostic, capable of transforming existing stereo matching networks (e.g., RAFT-Stereo) into transparent-aware sensors without architectural changes.
\end{itemize}

\section{RELATED WORK}

\subsection{Transparent Object Detection with RGB-D Sensors}

Standard RGB-D cameras fail to capture accurate depth for transparent objects 
due to light transmission and refraction. Depth completion methods aim to 
recover the corrupted depth structure caused by transparent objects through 
deep learning. These approaches can be categorized into two main strategies. 
The first, exemplified by Clear Grasp \cite{sajjan_clear_2020}, combines 
learned predictions (surface normals, masks, occlusion boundaries) with 
global optimization algorithms to reconstruct depth from over 50,000 synthetic 
training images. The second strategy, adopted by TransCG \cite{fang_transcg_2022} 
and ASGrasp \cite{shi_asgrasp_2024}, employs end-to-end networks that directly 
output completed depth maps. The key distinction between TransCG and ASGrasp 
lies in their input representation: TransCG retains the noisy depth map from 
RGB-D sensors as input, while ASGrasp discards the unreliable depth measurements 
entirely and directly utilizes RGB images paired with raw infrared (IR) stereo 
images to reconstruct geometry. Despite these different strategies, all methods 
remain data-intensive, requiring tens of thousands of training images to learn 
the complex mappings from corrupted or incomplete inputs to accurate depth.

\subsection{Polarization-Based Transparent Object Vision}

Polarization imaging reveals the geometric properties of transparent surfaces by measuring the polarization state of reflected light (degree of polarization $\rho$ and angle of polarization $\phi$), providing physical constraints for 3D reconstruction. Cui et al. \cite{cui_polarimetric_2017} explored polarimetric multi-view stereo for 3D modeling in static environments, achieving high-quality reconstruction by resolving polarization ambiguities through graph optimization. However, their multi-view acquisition (6-36 views $\times$ 7 images per view) requires several minutes, rendering it impractical for real-time applications. Zhu \& Smith \cite{zhu_depth_2019} simplified the setup to a stereo pair configuration (one polarization camera and one RGB camera), jointly solving polarization and stereo constraints to obtain metrically accurate depth maps. While this extends the applicability to broader scenarios, it still necessitates sequential capture of multiple polarization angle images and second-level optimization. Kalra et al. \cite{kalra_deep_2020} introduced deep learning as an alternative to traditional geometric optimization, employing a monocular multi-polarization camera (simultaneous capture of 4 polarization angles) to achieve real-time segmentation at 20 FPS. However, this approach requires 20,000 labeled training samples and outputs only 2D masks without depth information. All these methods rely on passive illumination, limiting their performance in low-light conditions, and exhibit inherent trade-offs among data efficiency, real-time capability, and 3D perception.

\section{METHOD}

We present a physics-informed framework for transparent obstacle avoidance that integrates asymmetric polarization cues into a standard stereo vision configuration. By exploiting the differential polarization response of surfaces to generate physically discriminative cues, the system preserves the geometric advantages of stereo while providing more informative inputs for learning. Rather than designing a new architecture from scratch, we treat the stereo network as a reusable backbone and adapt it to the polarization-encoded inputs via data-efficient fine-tuning. This eliminates the need for complex multi-view rigs or large-scale task-specific training datasets.

\subsection{System Overview}
\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[
        scale=0.55,
        transform shape,
        >=Stealth,
        polarizer/.style={fill=blue!30,draw=blue!60,opacity=0.7},
        detector/.style={fill=gray!40,draw=gray!70,thick},
        object/.style={fill=cyan!30,draw=cyan!60,opacity=0.5},
    ]

    % ===== Light Source =====
    \begin{scope}[shift={(0,0)}]
        % Optical axis
        \draw[thick,->,gray] (-0.5,0) -- (2.5,0);
        
        % Multiple sine waves for unpolarized light
        \foreach \angle/\col in {0/red,45/orange,90/green!70!black,135/cyan}{
            \draw[\col,thick,samples=100,domain=0:2] 
                plot (\x, {0.3*sin(\x*360)*cos(\angle)+0.3*cos(\x*360)*sin(\angle)*0.3});
        }
        
        % Label
        \node[above] at (0,1.2) {Light Source};
        
        % Cross-section (multi-directional arrows)
        \begin{scope}[shift={(-0.3,0)},canvas is yz plane at x=0,scale=2.0]
            \draw[thick] (0,0) circle (0.4);
            \draw[thick,->,red] (0,0) -- (0.35,0);
            \draw[thick,->,red] (0,0) -- (-0.35,0);
            \draw[thick,->,orange] (0,0) -- (0.247,0.247);
            \draw[thick,->,orange] (0,0) -- (-0.247,-0.247);
            \draw[thick,->,green!70!black] (0,0) -- (0,0.35);
            \draw[thick,->,green!70!black] (0,0) -- (0,-0.35);
            \draw[thick,->,cyan] (0,0) -- (-0.247,0.247);
            \draw[thick,->,cyan] (0,0) -- (0.247,-0.247);
        \end{scope}
    \end{scope}

    % ===== First Polarizer (0 deg) =====
    \begin{scope}[shift={(3,-0.15)}]
        \fill[polarizer] (-0.1,-1) rectangle (0.1,1);
        \foreach \y in {-0.8,-0.6,...,0.8}{
            \draw[blue!60,thin] (-0.1,\y) -- (0.1,\y);
        }
        \node[above] at (0,1.2) {Polarizer ($0^\circ$)};
    \end{scope}

    % ===== 0 deg Linearly Polarized Light going to object =====
    \begin{scope}[shift={(3.5,0)}]
        \draw[thick,->,gray] (0,0) -- (3.3,0);
        
        \draw[red,thick,samples=100,domain=0:3] 
            plot (\x, {0.4*sin(\x*360)});
    \end{scope}

    % ===== Transparent Object (light blue) =====
    \begin{scope}[shift={(7.2,0)}]
        \fill[object,rounded corners=2pt] (-0.15,-1.2) rectangle (0.15,1.2);
        \draw[cyan!60,thick,rounded corners=2pt] (-0.15,-1.2) rectangle (0.15,1.2);
        \node[right] at (0.3,1.0) {Transparent Object};
        
        % Reflection/refraction point
        \fill[yellow] (0,0) circle (0.1);
    \end{scope}

    % ===== Reflected light going back and splitting =====
    
    % ----- Upper path: reflected light -> Analyzer 0 deg -> Camera A -----
    % Reflected ray going up-left
    \begin{scope}[shift={(7.1,0)}]
        \draw[thick,->,gray] (0,0) -- (-2.7,1.98);
        
        \draw[red,thick,samples=50,domain=0:2.5] 
            plot ({-(\x)*cos(36.25)},{(\x)*sin(36.25)+0.35*sin(\x*400)*cos(36.25)});
    \end{scope}

    % Upper analyzer (0 deg) - perpendicular to light path
    \begin{scope}[shift={(4.5,2.2)},rotate=-53.75]
        \fill[polarizer] (-0.1,-0.6) rectangle (0.1,0.6);
        \foreach \y in {-0.4,-0.2,0,0.2,0.4}{
            \draw[blue!60,thin] (-0.1,\y) -- (0.1,\y);
        }
    \end{scope}
    \node[above] at (4.5,3.0) {Analyzer ($0^\circ$)};

    % Light passing through to Camera A (continuing same direction)
    \begin{scope}[shift={(3.9,2.35)}]
        \draw[thick,->,gray] (0,0) -- (-1.8,1.32);
        \draw[red,thick,samples=40,domain=0:1.6] 
            plot ({-(\x)*cos(36.25)},{(\x)*sin(36.25)+0.3*sin(\x*400)*cos(36.25)});
    \end{scope}

    % Camera A (perpendicular to light path)
    \begin{scope}[shift={(1.9,3.8)},rotate=-53.75]
        \fill[detector] (-0.15,-0.5) rectangle (0.15,0.5);
    \end{scope}
    \node[above] at (1.9,4.5) {Camera A};

    % ----- Lower path: reflected light -> Analyzer 90 deg -> Camera B -----
    % Reflected ray going down-left
    \begin{scope}[shift={(7.1,0)}]
        \draw[thick,->,gray] (0,0) -- (-2.7,-1.98);
        
        \draw[red,thick,samples=50,domain=0:2.5] 
            plot ({-(\x)*cos(36.25)},{-(\x)*sin(36.25)+0.35*sin(\x*400)*cos(36.25)});
    \end{scope}

    % Lower analyzer (90 deg) - perpendicular to light path
    \begin{scope}[shift={(4.5,-2.2)},rotate=53.75]
        \fill[polarizer] (-0.1,-0.6) rectangle (0.1,0.6);
        \foreach \x in {-0.05,0,0.05}{
            \draw[blue!60,thin] (\x,-0.6) -- (\x,0.6);
        }
    \end{scope}
    \node[below] at (4.5,-3.0) {Analyzer ($90^\circ$)};

    % Blocked light (dashed line, continuing same direction)
    \begin{scope}[shift={(3.9,-2.35)}]
        \draw[thick,->,gray,dashed] (0,0) -- (-1.8,-1.32);
    \end{scope}

    % Camera B (perpendicular to light path)
    \begin{scope}[shift={(1.9,-3.8)},rotate=53.75]
        \fill[detector] (-0.15,-0.5) rectangle (0.15,0.5);
    \end{scope}
    \node[below] at (1.9,-4.5) {Camera B};

    % ===== Transmitted light path (passing through transparent object) =====
    \begin{scope}[shift={(7.5,0)}]
        \draw[thick,->,gray] (0,0) -- (3,0);
        
        \draw[red,thick,samples=100,domain=0:2.7] 
            plot (\x, {0.4*sin(\x*360)});
    \end{scope}

    % Transmitted light label or detector (optional)
    \begin{scope}[shift={(10.8,0)}]
        \draw[thick,->] (0.2,0) -- (0.8,0);
        \node[right] at (0.9,0) {Transmitted};
    \end{scope}

    % ===== Optical axis =====
    \draw[gray,dashed,thin] (-1,0) -- (7,0);

    \end{tikzpicture}
    \caption{\textbf{Polarization imaging system schematic.} An active light source is polarized at $0^\circ$ via a linear polarizer. The stereo setup consists of Camera A (Parallel View, $I_{\parallel}$) equipped with a $0^\circ$ analyzer to capture specular reflections, and Camera B (Cross View, $I_{\perp}$) equipped with a $90^\circ$ analyzer to suppress them. This asymmetric configuration creates a distinct photometric discrepancy on transparent surfaces while maintaining intensity consistency on diffuse backgrounds.}
    \label{fig:polarization}
\end{figure}

We employ a physics-based asymmetric optical design to effectively decouple transparent object geometries from complex specular noise at the image-formation level. Specifically, we leverage the polarization-selective properties of linear polarizers. A source-side polarizer ensures that the emitted illumination maintains a fixed polarization state ($0^\circ$). Upon interaction with transparent or glossy surfaces, the specular reflection component becomes strongly polarized, whereas ambient and diffuse components remain largely unpolarized.

To exploit this physical behavior, we construct a hybrid stereo configuration. Unlike traditional setups that use identical filters for both views, we introduce an asymmetric filtering scheme: one camera is equipped with an analyzer parallel to the source ($0^\circ$, Parallel View), while the other is equipped with an analyzer orthogonal to the source ($90^\circ$, Cross View).

This configuration creates a selective photometric discrepancy. For unpolarized background regions, both cameras capture similar intensities due to diffuse reflection, allowing standard stereo matching algorithms to function correctly. However, for transparent surfaces, the Parallel View captures strong specular highlights, while the Cross View optically suppresses them. This mechanism effectively serves as a hardware-encoded attention signal: the difference between the left and right views explicitly highlights the transparent regions. Consequently, the complex inverse problem of transparency perception is simplified into a differential representation that is easier for neural networks to interpret.

As illustrated in \cref{fig:polarization}, our system integrates a structured-light projector with a source-side polarizer and a calibrated stereo camera pair with distinct polarization states. The outgoing light is pre-polarized, and the returning light is differentially filtered. Crucially, these asymmetric polarization-encoded images serve as a domain-agnostic input representation. By embedding material properties directly into the stereo pair, we ensure that downstream stereo-matching networks can achieve high performance through data-efficient fine-tuning, utilizing the discrepancy cues to localize transparent obstacles.

\subsection{Physics-Informed Input Formulation}

We decompose the illumination effects in real-world imaging into several components. Let $I(\mathbf{u})$ denote the measured image intensity at pixel location $\mathbf{u}$. This value is modeled as the linear superposition of three primary factors: diffuse reflection, specular reflection, and background illumination:
\begin{equation}
    I(\mathbf{u}) = I_{d}(\mathbf{u}) + I_{s}(\mathbf{u}) + I_{b}(\mathbf{u})
\end{equation}
To encode material properties into the stereo pair, we exploit the polarization-dependent nature of the specular term $I_s$. According to Malus's law, the intensity of the polarized specular component after filtering varies as $I_{s}^{filtered} \propto \cos^2\theta$, where $\theta$ is the relative angle between the source polarization and the analyzer.

Our asymmetric setup deliberately creates a photometric discrepancy by configuring the stereo cameras with distinct polarization states: a Parallel View ($I_{\parallel}$, $\theta=0^\circ$) and a Cross View ($I_{\perp}$, $\theta=90^\circ$).

For the Parallel View, the specular component is maximized. Assuming the unpolarized components ($I_d$ and $I_b$) are attenuated by a factor $\alpha$ (transmission coefficient, typically $\approx 0.5$) due to the polarizer, the formulation is:
\begin{equation}
    I_{\parallel}(\mathbf{u}) \approx \alpha \cdot (I_{d}(\mathbf{u}) + I_{b}(\mathbf{u})) + I_{s}(\mathbf{u})
    \label{eq:parallel_view}
\end{equation}
In contrast, for the Cross View, the analyzer is orthogonal to the source. The polarized specular reflection is effectively eliminated ($I_s \cdot \cos^2(90^\circ) \approx 0$), while the unpolarized components undergo the same attenuation $\alpha$:
\begin{equation}
    I_{\perp}(\mathbf{u}) \approx \alpha \cdot (I_{d}(\mathbf{u}) + I_{b}(\mathbf{u}))
    \label{eq:cross_view}
\end{equation}
This formulation reveals the core mechanism of our approach. By subtracting \cref{eq:cross_view} from \cref{eq:parallel_view}, the discrepancy implies $I_{\parallel} - I_{\perp} \approx I_{s}(\mathbf{u})$. Consequently, for background regions (where $I_s \approx 0$), the two views exhibit photometric consistency ($I_{\parallel} \approx I_{\perp}$), ensuring the validity of standard stereo matching. However, for transparent surfaces, the localized intensity difference ($I_{\parallel} \gg I_{\perp}$) serves as a robust physical cue for the network to identify obstacles.

\subsection{Standardized Backbone Utilization}

To demonstrate the universality of our approach, we adopt a model-agnostic strategy that treats the stereo network as a generic correspondence engine.

Our asymmetric acquisition setup produces inputs that maintain structural compatibility with standard RGB imagery while introducing a deliberate photometric discrepancy on transparent surfaces. Although this discrepancy violates the standard brightness constancy assumption locally, the background regions remain photometrically consistent ($I_{\parallel} \approx I_{\perp}$). This property ensures that the global geometric context is preserved, allowing standard stereo pipelines to establish reliable correspondence. Crucially, the local intensity mismatch ($I_{\parallel} \neq I_{\perp}$) acts as a strong discriminative cue, which Convolutional Neural Networks (CNNs) can learn to associate with the presence of transparent obstacles without requiring architectural modifications.

 A stereo neural architecture is required to process our binocular inputs. In this work, we specifically employ RAFT-Stereo \cite{lipson_raft-stereo_2021} as our primary backbone. Unlike traditional cost-volume approaches (e.g., PSMNet), RAFT-Stereo utilizes a recurrent architecture with Gated Recurrent Units (GRUs) to iteratively refine the disparity field.

This choice is particularly strategic for our asymmetric setup. The iterative mechanism allows the network to progressively resolve the photometric discrepancy between the parallel and cross views: early iterations establish global context from the background consistency ($I_{\parallel} \approx I_{\perp}$), while later iterations refine the object boundaries using the high-frequency structural cues provided by our polarization inputs. Furthermore, as a CNN-based architecture, it retains the inductive bias for locality, making it highly data-efficient for our fine-tuning paradigm compared to Transformer-based alternatives. Formally, the network inference is defined as:
\begin{equation}
    \hat{\mathbf{D}} = f_\theta(\mathbf{X}_{\parallel}, \mathbf{X}_{\perp})
\end{equation}
 To achieve the claimed data efficiency, we formulate the training as a transfer learning problem. Instead of random initialization, we initialize the network parameters $\theta_{init}$ with weights $\theta_{pre}$ pre-trained on a large-scale opaque dataset (e.g., Scene Flow):
\begin{equation}
    \theta_{init} \leftarrow \theta_{pre}
\end{equation}
The optimization objective is then to find the optimal parameters $\theta^*$ that minimize the task-specific loss $\mathcal{L}_{task}$ on our small-scale transparent dataset $\mathcal{D}_{trans}$:
\begin{equation}
    \theta^* = \mathop{\arg\min}_{\theta} \mathcal{L}_{task}(\mathcal{D}_{trans}; \theta_{init})
\end{equation}
Since our physics-informed inputs structurally align with the source domain of $\theta_{pre}$, the optimization starts from a favorable basin of attraction, requiring significantly fewer iterations to converge than training from scratch.

\subsection{Loss Function and Robustness Strategy}

\begin{equation}
    \mathcal{L} = \sum_{i=1}^{N} \gamma^{N-i} || \mathbf{D}_i - \mathbf{D}_{gt} ||_1
    \label{eq:lossfunction}
\end{equation}
As shown in \cref{eq:lossfunction}, we adopt the standard loss formulation used in RAFT-Stereo, where the total loss $\mathcal{L}$ is computed as a weighted sum of the Manhattan distances between the predicted disparity $\mathbf{D}_i$ at each iteration $i$ and the ground-truth disparity $\mathbf{D}_{gt}$. Compared to the Euclidean distance, the Manhattan distance corresponds to a Mean Absolute Error ($MAE$) formulation rather than Mean Squared Error ($MSE$), imposing a {linear penalty} on outliers rather than a quadratic one. This property is particularly beneficial in real-world transparent-object scenarios, where specular highlights and refraction effects may introduce occasional large residuals. By reducing the sensitivity to such outliers, the model exhibits improved robustness under realistic sensing disturbances.

The exponential weighting term $\gamma^{N-i}$ controls the relative importance of predictions at different recurrent iterations. This design emphasizes later predictions, which are expected to be more refined, while still providing supervisory signals to earlier iterations. The total loss is obtained by accumulating the weighted losses across all iterations, encouraging the network to optimize the entire iterative refinement process rather than focusing solely on the final output. As a result, the model learns to progressively resolve challenging disparity ambiguities introduced by transparent surfaces.

\section{EXPERIMENTS}

\begin{figure*}[t!]
\centering
\resizebox{0.9\textwidth}{!}{%
\begin{tikzpicture}[
    inputimg/.style={
        draw=inputblue,
        line width=1.2pt,
        rounded corners=2pt,
        inner sep=0pt,
        outer sep=0pt
    },
    encoder/.style={
        draw=networkpurple,
        line width=1pt,
        minimum width=2.6cm,
        minimum height=1.5cm,
        rounded corners=2pt,
        fill=blockfill,
        font=\small\sffamily,
        align=center
    },
    corrvolume/.style={
        draw=networkpurple,
        line width=1pt,
        minimum width=2.2cm,
        minimum height=2cm,
        rounded corners=2pt,
        fill=blockfill,
        font=\small\sffamily,
        align=center
    },
    gru/.style={
        draw=gruorange,
        line width=1.2pt,
        minimum width=2.4cm,
        minimum height=1.5cm,
        rounded corners=3pt,
        fill=grufill,
        font=\small\sffamily,
        align=center
    },
    outputimg/.style={
        draw=outputgreen,
        line width=1.2pt,
        rounded corners=2pt,
        inner sep=2pt
    },
    sectionlabel/.style={
        font=\large\bfseries\sffamily,
        text=black
    },
    imglabel/.style={
        font=\small\sffamily,
        text=black,
        align=center
    },
    arrow/.style={
        ->,
        >={Stealth[length=2.5mm, width=2mm]},
        line width=1.2pt,
        arrowblue
    },
    bigarrow/.style={
        ->,
        >={Stealth[length=3.5mm, width=2.5mm]},
        line width=1.8pt,
        arrowblue
    }
]

% ============================================
% INPUT SECTION
% ============================================
\node[sectionlabel] at (-1, 4) {\textbf{INPUT}};

% Parallel View - 插入你的圖片
\node[inner sep=0pt] (img_parallel) at (-1, 1.5) {\includegraphics[width=2.8cm, height=2cm]{paper//Figures/IMG_for_testing.png}};
\node[imglabel] at (-1, 2.75) {Parallel View ($I_\parallel$)};

% Cross View - 插入你的圖片
\node[inner sep=0pt] (img_cross) at (-1, -1) {\includegraphics[width=2.8cm, height=2cm]{paper//Figures/IMG_for_testing.png}};
\node[imglabel] at (-1, 0.25) {Cross View ($I_\perp$)};

% ============================================
% NETWORK SECTION
% ============================================
\node[sectionlabel] at (7.4, 4) {\textbf{NETWORK}};
\node[font=\small\sffamily\itshape, text=gray!70!black] at (7.4, 3.45) {(RAFT-Stereo)};

% Feature Encoder - Top
\node[encoder] (enc_top) at (3.8, 1.5) {Feature Encoder\\{\scriptsize(multi-scale)}};

% Feature Encoder - Bottom
\node[encoder] (enc_bot) at (3.8, -1) {Feature Encoder\\{\scriptsize(multi-scale)}};

% Shared weights
\draw[dashed, networkpurple!60, line width=0.8pt] (enc_top.south) -- (enc_bot.north);
\node[font=\scriptsize\sffamily, text=networkpurple!70] at (4.25, 0.25) {shared};

% Correlation Volume
\node[corrvolume] (corr) at (7.2, 0.25) {Correlation\\Volume};

% GRU Update
\node[gru] (gru) at (11, 0.25) {GRU Update};

% Recurrence arrow
\draw[->, >={Stealth[length=2mm, width=1.5mm]}, line width=1pt, gruorange] 
    ([xshift=0.6cm]gru.north) 
    .. controls ++(0, 0.8) and ++(0, 0.8) .. 
    ([xshift=-0.6cm]gru.north);

% ×N
\node[font=\scriptsize\bfseries\sffamily, text=gruorange] at (11, 2.0) {$\times N$};

% Update equation
\node[font=\scriptsize\sffamily, text=gruorange!80] at (11, -1.3) {$D_{i+1} = D_i + \Delta D_i$};

% ============================================
% OUTPUT SECTION
% ============================================
\node[sectionlabel] at (15.5, 4) {\textbf{OUTPUT}};

% Disparity Map - 插入你的圖片
\node[inner sep=0pt] (output) at (15.5, 0.25) {\includegraphics[width=3cm, height=2.4cm]{paper//Figures/IMG_for_testing.png}};
\node[imglabel] at (15.5, 1.7) {Disparity Map ($\hat{D}$)};

% ============================================
% ARROWS
% ============================================
\draw[bigarrow] (img_parallel.east) -- (enc_top.west);
\draw[bigarrow] (img_cross.east) -- (enc_bot.west);
\draw[arrow] (enc_top.east) -- ++(0.5, 0) |- (corr.west);
\draw[arrow] (enc_bot.east) -- ++(0.5, 0) |- (corr.west);
\draw[arrow] (corr.east) -- (gru.west);
\draw[bigarrow] (gru.east) -- (output.west);

% ============================================
% BACKGROUND BOXES
% ============================================
\begin{scope}[on background layer]
    \node[fit=(img_parallel)(img_cross), 
          fill=inputblue!6, 
          draw=inputblue!30, 
          line width=0.5pt,
          rounded corners=6pt,
          inner sep=20pt] {};
    
    \node[fit=(enc_top)(enc_bot)(corr)(gru), 
          fill=networkpurple!5, 
          draw=networkpurple!25, 
          line width=0.5pt,
          rounded corners=6pt,
          inner sep=25pt] {};
    
    \node[fit=(output), 
          fill=outputgreen!6, 
          draw=outputgreen!30, 
          line width=0.5pt,
          rounded corners=6pt,
          inner sep=20pt] {};
\end{scope}

\end{tikzpicture}%
}%
\caption{\textbf{Overview of the proposed PIDS framework.} The system takes a pair of active asymmetric polarization images as input: the parallel view ($I_{\parallel}$) captures specular reflections, while the cross view ($I_{\perp}$) filters them out. Features are extracted using weight-sharing Siamese encoders to ensure geometric consistency for background regions. Based on the RAFT-Stereo backbone, the network constructs a correlation volume and employs GRU units for iterative disparity updates ($D_{i+1} = D_i + \Delta D$). This mechanism allows the network to leverage the photometric discrepancy at the input level as a physical cue to accurately refine depth estimates for transparent objects.}
\label{fig:system_overview}
\end{figure*}
\subsection{Experiment Setup}
\subsubsection{Hardware and Layout}
\subsubsection{Radiometric Calibration}

\subsection{Training Details and Baselines}
\begin{figure}[h!] 
\centering
\begin{tikzpicture}[
    scale=0.75, transform shape,
    font=\sffamily\scriptsize,
    node distance=0.6cm and 0.4cm,
    % --- Style Definitions ---
    block/.style={
        rectangle, 
        draw=black!60, 
        thick, 
        fill=white, 
        minimum width=2.0cm,
        minimum height=0.9cm,
        align=center, 
        rounded corners=2pt,
        drop shadow={opacity=0.15, shadow xshift=1pt, shadow yshift=-1pt}
    },
    data/.style={
        trapezium, 
        trapezium left angle=75, 
        trapezium right angle=105, 
        draw=black!60, 
        thick, 
        fill=white, 
        align=center, 
        inner sep=4pt
    },
    network/.style={
        rectangle, 
        draw=blue!60, 
        line width=1.2pt, 
        fill=blue!10, 
        minimum width=2.3cm, 
        minimum height=0.9cm, 
        align=center, 
        rounded corners=4pt
    },
    arrow/.style={-{Stealth[length=2mm, width=1.5mm]}, thick, darkgray},
    loss_arrow/.style={-{Stealth[length=2mm, width=1.5mm]}, thick, red!70, dashed},
    dashed_arrow/.style={-{Stealth[length=2mm, width=1.5mm]}, thick, blue!80, dashed},
]

    % ==========================================
    % STAGE 1: SIMULATION (Top)
    % ==========================================
    
    \node[font=\bfseries\small\color{blue!70}] (label_s1) {Stage I: Large-scale synthetic pre-training};
    
    \node[block, below=0.4cm of label_s1] (blender) {
        \textbf{Blender Sim}\\
        \tiny Physics-Aligned
    };
    
    \node[data, below=0.5cm of blender] (sim_data) {
        \textbf{Sim Data}\\
        $(I_{\parallel}, I_{\perp})_{sim}$, $D_{sim}$
    };
    
    \node[network, below=0.5cm of sim_data] (net_pre) {
        \textbf{Pre-train Net}\\
        \tiny Learn Polarization Cues
    };

    % Connections
    \draw[arrow] (blender) -- (sim_data);
    \draw[arrow] (sim_data) -- (net_pre);

    % ==========================================
    % STAGE 2: REAL WORLD (Bottom)
    % ==========================================
    
    \node[below=1.2cm of net_pre, font=\bfseries\small\color{orange!80}] (label_s2) {Stage II: Limited real-world fine-tuning};
    
    \node[block, below=0.4cm of label_s2] (real_setup) {
        \textbf{Real Platform}\\
        \tiny Hardware Setup
    };
    
    \node[block, below left=0.5cm and 0.2cm of real_setup] (capture_glass) {
        \textbf{Capture A}\\
        \tiny (Transparent)\\
        \tiny Filtered
    };
    
    \node[block, below right=0.5cm and 0.2cm of real_setup] (capture_proxy) {
        \textbf{Capture B}\\
        \tiny (Proxy)\\
        \tiny RGB-D
    };
    
    \node[data, below=0.5cm of capture_glass, fill=blue!5] (real_input) {
        $(I_{\parallel}, I_{\perp})_{real}$
    };
    
    \node[data, below=0.5cm of capture_proxy, fill=red!5] (real_gt) {
        \textbf{Ground Truth}\\
        $D_{gt}$
    };
    
    \node[network, below=0.6cm of $(real_input.south)!0.5!(real_gt.south)$, draw=orange!60, fill=orange!10] (net_real) {
        \textbf{Correction Net}\\
        \tiny Fine-tuning
    };

    % --- Connections ---
    \draw[arrow] (real_setup.south) -- ++(0,-0.15) -| (capture_glass.north);
    \draw[arrow] (real_setup.south) -- ++(0,-0.15) -| (capture_proxy.north);
    \draw[arrow] (capture_glass) -- (real_input);
    \draw[arrow] (capture_proxy) -- (real_gt);
    
    % Input Arrow
    \draw[arrow] (real_input.south) -- ++(0,-0.15) -| (net_real.north);
    
    % ★★★ 修改處：Loss Arrow & Label ★★★
    % 1. 先下移 0.15cm
    % 2. 定義轉折點 (corner)
    % 3. 畫橫線到對齊 net_real 的位置，並在中間上方 (midway, above) 加上 Loss 標籤
    % 4. 最後垂直向下連接
    \draw[loss_arrow] (real_gt.south) -- ++(0,-0.15) coordinate (corner) 
        -- (corner -| net_real.north) node[midway, above, font=\bfseries\tiny, color=red!70, yshift=-1pt, xshift=-0.5cm] {Loss} 
        -- (net_real.north);
    
    % 移除了原本獨立的 \node {Loss}; 避免重複

    % ==========================================
    % BACKGROUNDS & TRANSFER ARROW
    % ==========================================
    \begin{scope}[on background layer]
        \node[fit=(label_s1)(blender)(sim_data)(net_pre), fill=blue!5, rounded corners=5pt, inner sep=6pt] (sim_box) {};
        \draw[blue!30, thick, rounded corners=5pt] (sim_box.south west) rectangle (sim_box.north east);
        
        \node[fit=(label_s2)(real_setup)(capture_glass)(capture_proxy)(real_input)(real_gt)(net_real), fill=orange!5, rounded corners=5pt, inner sep=6pt] (real_box) {};
        \draw[orange!30, thick, rounded corners=5pt] (real_box.south west) rectangle (real_box.north east);
    \end{scope}

    % Weights Transfer Arrow
    \draw[dashed_arrow] (sim_box.south -| net_pre) -- (real_box.north -| net_pre)
        node[midway, right, font=\bfseries\tiny\color{blue!80}, xshift=2pt, align=left] {Weights\\Transfer};


\end{tikzpicture}
\caption{\textbf{Two-Stage Fine-tuning Framework.} 
\textbf{Stage I:} The stereo network is pre-trained on large-scale physics-aligned synthetic data generated in Blender to learn polarization-induced photometric cues.
\textbf{Stage II:} the same network is fine-tuned on a limited amount of real-world data.
Ground-truth depth $D_{gt}$ is obtained using an opaque proxy replacement strategy, where transparent objects are temporarily replaced by opaque counterparts to enable reliable RGB-D sensing.Importantly, the network input always consists of real transparent polarization images, while proxy objects are used solely for supervision.}
\label{fig:fine_tuning_pipeline}
\end{figure}

\subsection{Comparison with Baseline RAFT-Stereo}
\subsection{Controlled Micro-Scale Depth Completeness Evaluation}
\subsection{Real-World Robot Evaluation}

\section{RESULTS}
\section{CONCLUSIONS}
\section*{APPENDIX}
\bibliographystyle{IEEEtran.bst}  % 指定使用 IEEE 標準格式
\bibliography{IEEEfull.bib}










\end{document}