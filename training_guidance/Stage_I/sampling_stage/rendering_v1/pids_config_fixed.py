"""
PIDS 配置檔 v2.0
================

所有場景生成和渲染的參數設定
"""

# ============================================================
# 場景配置 (Blender 使用)
# ============================================================

SCENE_CONFIG = {
    # 場景範圍 (mm) - Blender 座標系
    # X: 左右 (-width/2 ~ +width/2)
    # Y: 深度 (朝向相機方向為正)
    # Z: 高度 (向上為正)
    
    'width_mm': 250,              # 場景寬度
    'glass_y_min_mm': 528,        # 玻璃最近距離
    'glass_y_max_mm': 695,        # 玻璃最遠距離  
    'furniture_y_mm': 750,        # 傢俱基準深度
    'furniture_y_jitter_mm': 50,  # 傢俱深度隨機範圍
    
    # 碰撞檢測
    'collision_margin_mm': 15,
}

# ============================================================
# 相機配置
# ============================================================

CAMERA_CONFIG = {
    'resolution': (640, 480),
    'fov_horizontal_deg': 55.0,   # IMX296 + 6mm 鏡頭: 2*arctan(6.3/(2*6)) ≈ 55°
    'baseline_mm': 65.0,
    
    # 工作距離
    'min_distance_mm': 500,
    'max_distance_mm': 750,
}

# ============================================================
# 光源配置
# ============================================================

LIGHT_CONFIG = {
    'width_mm': 400,
    'height_mm': 300,
    
    # 光源位置 (Mitsuba 座標: X左右, Y高度, Z深度)
    # 放在場景上方，朝向物件照射
    'position_m': [0, 0.5, 0.4],  # 場景上方中央
    
    # 入射角度 (光源與相機光軸夾角 35°)
    # 光線入射角 ≈ 55° (接近 Brewster angle)
    'angle_deg': -35,
    
    # 光源強度 (增加以確保足夠亮度)
    'radiance': 300.0,
}

# ============================================================
# 渲染配置
# ============================================================

RENDER_CONFIG = {
    'samples': 128,      # 採樣數 (越高越少噪點)
    'max_depth': 8,      # 光線追蹤深度
}

# ============================================================
# 材質配置
# ============================================================

# 傢俱顏色 (RGB, 0~1)
MATERIAL_COLORS = {
    'shelf':      [0.35, 0.25, 0.15],  # 深木色
    'cabinet':    [0.50, 0.40, 0.30],  # 淺木色
    'table':      [0.45, 0.35, 0.25],  # 橡木色
    'chair':      [0.40, 0.30, 0.20],  # 胡桃木色
    'sofa':       [0.30, 0.30, 0.35],  # 灰藍色
    'default':    [0.50, 0.45, 0.40],  # 預設
    'background': [0.75, 0.70, 0.65],  # 背景牆 (米色)
    'ground':     [0.45, 0.40, 0.35],  # 地面 (木地板)
}

# 玻璃折射率
GLASS_IOR = 1.5

# ============================================================
# 玻璃物件類型
# ============================================================

# size: (寬度, 高度, 厚度) mm
GLASS_TYPES = [
    {'name': 'door',      'size': (90, 150, 2)},
    {'name': 'window',    'size': (100, 80, 2)},
    {'name': 'partition', 'size': (50, 40, 2)},
    {'name': 'panel',     'size': (60, 60, 2)},
]

GLASS_COUNT_RANGE = (1, 3)
GLASS_ROTATION_Z_DEG = (-30, 30)
GLASS_TILT_X_DEG = (-5, 5)

# ============================================================
# 傢俱物件類型
# ============================================================

# size: (寬度, 深度, 高度) mm
FURNITURE_TYPES = [
    {'name': 'shelf',   'size': (80, 30, 180)},
    {'name': 'cabinet', 'size': (100, 40, 90)},
    {'name': 'table',   'size': (80, 50, 45)},
    {'name': 'chair',   'size': (45, 45, 85)},
    {'name': 'sofa',    'size': (120, 60, 70)},
]

FURNITURE_COUNT_RANGE = (2, 4)
FURNITURE_ROTATION_Z_DEG = (-15, 15)
FURNITURE_SCALE_RANGE = (0.6, 1.05)

# ============================================================
# 輸出路徑
# ============================================================

OUTPUT_CONFIG = {
    'blender_output_dir': '/tmp/pids_scenes',
    'render_output_dir': '/tmp/pids_renders',
}
